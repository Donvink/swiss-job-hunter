// Scaffolding only — sub-nav (UPCOMING | SEARCH | RESUMES | STORIES) and
// child components land in later phases.
export default function InterviewsPage({ onSelectJob }) {
  return (
    <div style={{flex:1,display:"flex",alignItems:"center",justifyContent:"center",color:"#c4beb0",fontSize:12}}>
      INTERVIEWS — coming soon
    </div>
  );
}
